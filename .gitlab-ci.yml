stages:
  - build
  - test 

build website:
  stage: build	
  image: node
  script:
    - npm install
    - npm run build
  artifacts:
    paths:
      - public

test artifact:
  image: alpine
  stage: test
  script:
    - grep "React" ./dist/index.html; then
       
